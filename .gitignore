# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

.pytest_cache/
.ruff_cache/

.env

# DVC cache (data stored in MinIO remote)
.dvc/cache/
.dvc/tmp/
.dvc/config.local

# DVC processed data (tracked by DVC, stored in MinIO)
# The .dvc files must be in Git, but the actual data is in MinIO
data/processed/*
!data/processed/.gitignore
!data/processed/*.dvc

# MLflow local data (legacy - now using PostgreSQL + MinIO)
# These files are kept as backup but no longer used in the new architecture
mlflow.db
mlruns/

# Claude Code
.claude

# Personal files
CHECKLIST.md
.coverage
PLAN_MLOPS.md
PROJECT_REPORT.md
*.docx

*.pdf