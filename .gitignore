# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

.pytest_cache/
.ruff_cache/

.env

# DVC cache (data stored in MinIO remote)
.dvc/cache/
.dvc/tmp/
.dvc/config.local

# DVC processed data (local MinIO, not shared)
# Both data and .dvc files are ignored since MinIO is ephemeral
data/processed/

# MLflow local data (legacy - now using PostgreSQL + MinIO)
# These files are kept as backup but no longer used in the new architecture
mlflow.db
mlruns/

# Claude Code
.claude

# Personal files
CHECKLIST.md
.coverage
PLAN_MLOPS.md
PROJECT_REPORT.md
*.docx

*.pdf